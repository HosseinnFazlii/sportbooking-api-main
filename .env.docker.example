# API
PORT=3030
UPLOAD_DIR=/home/node/uploads

# DB: addressable by container name on the same Docker network
# DB values for the API (DB_HOST is overridden to 'db' by docker-compose)
DB_HOST=psql-db
DB_PORT=5432
DB_USER=psql_admin
DB_PASS=SOME_PASS
DB_NAME=SportBooking
DB_SSL=false

# JWT
JWT_SECRET=jwt_secret
JWT_EXPIRES_IN=86400s
